import api from './api';
import events from './events';
import net from './net';
import utils from './utils';


const directual = {
  /**
   * @type {ApiClient}
   */
  api,
  events,
  net,
  utils,
};

export default directual;
